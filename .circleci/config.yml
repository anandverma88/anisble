version: 2
anchors:  
jobs:
  test:
    machine:
      image: ubuntu-2004:current
    steps: 
      - run: 
          name: Installing prerequiste
          command: |
            sudo apt-get update
            sudo apt install python3-pip
            sudo apt install git
            pip3 install ansible
            git clone https://github.com/anandverma88/anisble.git
            cd ansible
            ansible-playbook test.yml
            pwd
            ls -lart


workflows:
  version: 2
  build:
    jobs:
      - test